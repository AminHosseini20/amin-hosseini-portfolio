# تحویل پروژه Personal Portfolio

کد کامل وب‌سایت در ریشه‌ی همین پوشه‌ی پروژه قرار دارد. برای اجرا، `pnpm install` و سپس `pnpm dev` را اجرا کنید؛ برای خروجی نهایی از `pnpm build` استفاده کنید.

اطلاعات رزومه در `src/data/profile.ts` جدا شده تا به‌سادگی قابل تغییر باشد.
